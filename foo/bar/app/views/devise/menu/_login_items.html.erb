<% if user_signed_in? %>
  <li>
  <%= button_to("Log Out", destroy_user_session_path, method: :delete) %>
  </li>
<% else %>
  <li>
  <%= button_to("Sign In", new_user_session_path) %>
  </li>
<% end %>